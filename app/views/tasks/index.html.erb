<h1>Votre liste de choses à faire:</h1>

<table>
<% @tasks.each do |task| %>
  <tr>
    <td><%= task.task %></td>
    <td><%= task.status %></td>
    <td><%= link_to "Edit", edit_task_path(task) %></td>
    <td><%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>
<% end %>
</table>
